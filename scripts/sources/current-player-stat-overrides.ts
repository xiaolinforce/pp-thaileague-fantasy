/**
 * Reviewed identity matches only. Keys are stable Thai League person IDs and
 * values are stable Transfermarkt player IDs. Never add a name-only override.
 */
export const currentPlayerStatOverrides: Record<string, string> = {};
